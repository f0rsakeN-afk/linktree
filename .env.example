# ─────────────────────────────────────────────────────────────────────────────
# LinkDrop — Environment Variables
# Copy this file to .env and fill in your values.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database ─────────────────────────────────────────────────────────────────
# PostgreSQL connection string (Neon, Supabase, Railway, local PG, etc.)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# ── Security ─────────────────────────────────────────────────────────────────
# CRITICAL: Generate with `openssl rand -hex 32`
# Used to HMAC-derive access keys and backup codes.
# Never commit the real value. Changing this invalidates ALL existing keys.
HMAC_SECRET="replace-with-openssl-rand-hex-32-output"

# ── App ───────────────────────────────────────────────────────────────────────
# Public base URL (used for generating shareable profile URLs)
NEXT_PUBLIC_BASE_URL="https://yourdomain.com"
